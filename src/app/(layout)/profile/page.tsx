import { memo } from "react";

const Profile = () => {
  return (
    <div className="w-[100%] min-h-[400px] flex items-center justify-center">
      <div>
        profile
    </div>
    </div>
  );
};

export default memo(Profile);
